{
  "insecure-registries": [
    "{{ $.hostIP }}:{{ $.dory.artifactRepo.internal.portHub }}",
    "{{ $.hostIP }}:{{ $.dory.artifactRepo.internal.portGcr }}",
    "{{ $.hostIP }}:{{ $.dory.artifactRepo.internal.portQuay }}"
  ]
}
