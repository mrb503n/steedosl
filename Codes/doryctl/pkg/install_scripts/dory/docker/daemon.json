{
  "insecure-registries": [
    "{{ $.hostIP }}:{{ $.dory.artifactRepo.portHub }}",
    "{{ $.hostIP }}:{{ $.dory.artifactRepo.portGcr }}",
    "{{ $.hostIP }}:{{ $.dory.artifactRepo.portQuay }}"
  ]
}
